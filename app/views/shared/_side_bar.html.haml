.side-bar
  .side-bar__user-name
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        =link_to new_group_path, class: "top-items__icons, top-items__icon" do
          =icon('fas', 'edit', class: "top-items__icons--edit")
        =link_to edit_user_path(current_user), class: "top-items__icons, top-items__icon" do
          =icon('fas', 'cog', class: "top-items__icons--cog")
  .side-bar__group-list
    - current_user.groups.each do |group|
      .group-list
        =link_to "#" do
          .group-list__name
            = group.name
          .group-list__message
            まだメッセージがありません